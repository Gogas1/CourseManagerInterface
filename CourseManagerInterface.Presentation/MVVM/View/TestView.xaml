<UserControl x:Class="CourseManagerInterface.Presentation.MVVM.View.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourseManagerInterface.Presentation.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid 
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <StackPanel>
            <TextBox 
                Width="100"
                Margin="0 0 0 10"
                Text="{Binding SomeString, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox 
                Width="100" 
                Margin="0 0 0 10"
                Text="{Binding SomeInt, UpdateSourceTrigger=PropertyChanged}"/>
            <Button 
                Content="Send"
                Margin="0 0 0 10"
                Command="{Binding SendMessageCommand}"/>
            <TextBox 
                Text="{Binding RequestedDateTime, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>
    </Grid>
</UserControl>
